{
  "name": "crazsix-stock",
  "version": "1.0.0",
  "private": true,
  "description": "Crazsix inventory and accounting dashboard",
  "scripts": {
    "test": "playwright test --config tests/playwright.config.js",
    "test:ui": "playwright test --config tests/playwright.config.js --ui",
    "serve": "node scripts/test-server.js",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "chart.js": "4.4.4"
  }
}
